{
  "private": true,
  "scripts": {
    "start": "npm install && gulp",
    "build": "npm install && gulp build"
  },
  "devDependencies": {
    "typescript": "latest",
    "gulp": "latest",
    "gulp-transform": "latest",
    "gulp-rename": "latest",
    "gulp-merge": "latest",
    "gulp-typescript": "latest",
    "gulp-uglify": "latest",
    "gulp-open": "latest"
  }
}